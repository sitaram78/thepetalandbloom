import { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import { ArrowRight, Star, Heart, Clock, Palette, Truck, Gift, Sparkles } from 'lucide-react';
import Reveal from '@/components/Reveal';
import SectionHeading from '@/components/SectionHeading';
import ProductCard from '@/components/ProductCard';
import WhatsAppButton from '@/components/WhatsAppButton';
import SEO from '@/components/SEO';
import {
  products, getBestsellers, formatPrice,
} from '@/data/products';
import {
  occasions, trustStrip, bouquetTiers, addOns, giftingOccasions,
  instagramPosts, heroImages, brandInfo, budgetFilters,
  howItWorksSteps, faqs,
} from '@/data/site';
import { customOrderMessage } from '@/utils/whatsapp';
import { trackEvent } from '@/utils/analytics';

const trustIcons: Record<string, typeof Heart> = {
  Heart, Clock, Palette, Truck, Gift,
};

export default function Home() {
  const bestsellers = getBestsellers();
  const featuredProduct = products.find((p) => p.code === 'TPB-BQ-003')!;

  useEffect(() => {
    trackEvent('homepage_view');
  }, []);

  return (
    <div className="overflow-hidden bg-parchment-50">
      <SEO
        title="Handmade Crochet Flowers, Bouquets & Gifts"
        description="Handmade crochet flowers, bouquets, and gifts made in India. Custom colours, made to order, pan-India delivery. Flowers that never fade."
        canonicalPath="/"
        structuredData={{
          '@context': 'https://schema.org',
          '@type': 'Store',
          name: 'The Petal & Bloom',
          description: 'Handmade crochet flowers, bouquets, and gifts made in India.',
          url: 'https://petal-bloom-d2c-webs-k5dq.bolt.host',
        }}
      />

      {/* 01. THE THESIS: Hero Section */}
      <section className="relative min-h-screen flex items-center justify-center bg-parchment-100">
        <div className="absolute inset-0">
          <img
            src={heroImages.primary}
            alt="Handmade crochet bouquet"
            className="w-full h-full object-cover animate-gentle-zoom"
          />
          <div className="absolute inset-0 bg-gradient-to-b from-parchment-50/40 via-transparent to-parchment-50" />
        </div>

        <div className="container-lux relative z-10 text-center">
          <Reveal>
            <p className="section-label mb-6 tracking-[0.3em] animate-fade-up">Handmade in India</p>
            <h1 className="heading-serif text-6xl sm:text-7xl lg:text-8xl text-ink text-balance animate-fade-up leading-[1.1]" style={{ animationDelay: '0.2s' }}>
              Flowers that<br />never fade.
            </h1>
            <p className="mt-8 text-lg sm:text-xl text-ink-light leading-relaxed max-w-2xl mx-auto animate-fade-up" style={{ animationDelay: '0.4s' }}>
              Hand-sculpted crochet blooms and thoughtful gifts, created slowly to be given meaningfully.
            </p>
            <div className="mt-12 flex flex-col sm:flex-row items-center justify-center gap-4 animate-fade-up" style={{ animationDelay: '0.6s' }}>
              <Link to="/shop" className="btn-primary text-lg px-10 py-4">
                Explore the Studio <ArrowRight size={18} />
              </Link>
              <Link to="/custom-bouquet" className="btn-secondary text-lg px-10 py-4">
                Co-create a Bouquet
              </Link>
            </div>
          </Reveal>
        </div>
      </section>

      {/* 02. THE PHILOSOPHY: Emotional Connection */}
      <section className="py-24 lg:py-32 bg-parchment-50">
        <div className="container-lux">
          <div className="max-w-3xl mx-auto text-center">
            <Reveal>
              <h2 className="heading-serif text-4xl lg:text-5xl text-ink mb-8 leading-tight">
                An Heirloom of Affection.
              </h2>
              <p className="text-lg text-ink-light leading-relaxed mb-6">
                Fresh flowers are a fleeting beauty. We believe in a different kind of bloom — one that carries the memory of the moment it was given, indefinitely.
              </p>
              <p className="text-xl font-serif italic text-rose">
                Every petal is shaped stitch by stitch, turning yarn into a permanent symbol of love.
              </p>
            </Reveal>
          </div>
        </div>
      </section>

      {/* 03. THE CURATION: Mood-based discovery */}
      <section className="py-24 lg:py-32 bg-silk">
        <div className="container-lux">
          <SectionHeading
            label="The Collections"
            title="Curated for your moments."
            subtitle="Whether it's a quiet thank you or a grand celebration, find the mood that matches yours."
          />
          <div className="grid grid-cols-1 md:grid-cols-3 gap-8 mt-16">
            {occasions.slice(0, 3).map((occasion, i) => {
              const Icon = occasionIcons[occasion.icon] || Heart;
              return (
                <Reveal key={occasion.name} delay={i * 100}>
                  <Link
                    to={`/shop?occasion=${occasion.filter}`}
                    className="group relative block overflow-hidden rounded-sm aspect-[4/5] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-rose"
                  >
                    <img
                      src={occasion.image}
                      alt={occasion.imageAlt}
                      className="w-full h-full object-cover transition-transform duration-1000 group-hover:scale-110"
                    />
                    <div className="absolute inset-0 bg-gradient-to-t from-ink/80 via-ink/20 to-transparent" />
                    <div className="absolute bottom-0 left-0 right-0 p-8 text-left">
                      <div className={`w-10 h-10 rounded-full ${occasion.emojiBg} flex items-center justify-center mb-4`}>
                        <Icon size={20} className={occasion.emojiColor} strokeWidth={1.5} />
                      </div>
                      <h3 className="font-serif text-2xl text-parchment-50">{occasion.name}</h3>
                      <p className="text-sm text-parchment-100/80 mt-2 leading-snug">
                        {occasion.description}
                      </p>
                    </div>
                  </Link>
                </Reveal>
              );
            })}
          </div>
          <div className="text-center mt-16">
            <Link to="/shop" className="btn-secondary">
              View all Collections <ArrowRight size={16} />
            </Link>
          </div>
        </div>
      </section>

      {/* 04. THE STUDIO EXPERIENCE: Custom Bouquet */}
      <section className="py-24 lg:py-32 bg-parchment-50">
        <div className="container-lux">
          <div className="grid lg:grid-cols-2 gap-16 items-center">
            <Reveal>
              <div className="relative aspect-square overflow-hidden rounded-sm bg-silk">
                <img
                  src={heroImages.secondary}
                  alt="Custom Bouquet Process"
                  className="w-full h-full object-cover"
                />
                <div className="absolute inset-0 bg-rose/10 mix-blend-multiply" />
              </div>
            </Reveal>
            <Reveal delay={100}>
              <p className="section-label mb-4">The Studio Table</p>
              <h2 className="heading-serif text-4xl lg:text-6xl text-ink mb-6 leading-tight">
                Your flowers.<br />Your story.
              </h2>
              <p className="text-lg text-ink-light leading-relaxed mb-8">
                Not every emotion fits a standard bouquet. We invite you to co-create a piece of botanical art — choosing every bloom, every hue, and every detail.
              </p>
              <div className="flex flex-col sm:flex-row gap-4">
                <Link to="/custom-bouquet" className="btn-primary px-8 py-4">
                  Start Co-creating <ArrowRight size={18} />
                </Link>
                <WhatsAppButton
                  message={customOrderMessage()}
                  label="Consult the Studio Assistant"
                  variant="outline"
                  className="px-8 py-4"
                />
              </div>
            </Reveal>
          </div>
        </div>
      </section>

      {/* 05. THE PROOF: Craftsmanship Macro */}
      <section className="py-24 lg:py-32 bg-ink text-parchment-50">
        <div className="container-lux">
          <div className="text-center max-w-2xl mx-auto mb-16">
            <Reveal>
              <p className="section-label text-rose mb-4">The Detail</p>
              <h2 className="heading-serif text-4xl lg:text-5xl mb-6">Stitch by Stitch.</h2>
              <p className="text-parchment-100/60 text-lg leading-relaxed">
                Luxury is found in the details. Every petal is sculpted by hand, ensuring that no two blooms are identical — just like the people they are given to.
              </p>
            </Reveal>
          </div>
          <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
            <Reveal delay={100}>
              <div className="aspect-[4/5] overflow-hidden rounded-sm group relative">
                <img src={heroImages.hands} alt="Hand crafting" className="w-full h-full object-cover transition-transform duration-700 group-hover:scale-105" />
                <div className="absolute inset-0 bg-ink/20 group-hover:bg-transparent transition-all duration-500" />
              </div>
            </Reveal>
            <Reveal delay={200}>
              <div className="aspect-[4/5] overflow-hidden rounded-sm group relative mt-12">
                <img src={heroImages.texture} alt="Yarn detail" className="w-full h-full object-cover transition-transform duration-700 group-hover:scale-105" />
                <div className="absolute inset-0 bg-ink/20 group-hover:bg-transparent transition-all duration-500" />
              </div>
            </Reveal>
            <Reveal delay={300}>
              <div className="aspect-[4/5] overflow-hidden rounded-sm group relative">
                <img src={heroImages.yarn} alt="Yarn palette" className="w-full h-full object-cover transition-transform duration-700 group-hover:scale-105" />
                <div className="absolute inset-0 bg-ink/20 group-hover:bg-transparent transition-all duration-500" />
              </div>
            </Reveal>
          </div>
        </div>
      </section>

      {/* 06. THE TRUST STRIP: Minimalist */}
      <section className="py-12 bg-parchment-50 border-b border-silk">
        <div className="container-lux">
          <div className="flex flex-wrap items-center justify-center gap-x-12 gap-y-6 opacity-70">
            {trustStrip.map((item, i) => {
              const Icon = trustIcons[item.icon] || Heart;
              return (
                <div key={i} className="flex items-center gap-3 text-ink-light">
                  <Icon size={18} strokeWidth={1.5} className="text-rose" />
                  <span className="text-xs sm:text-sm font-medium tracking-wide uppercase">{item.label}</span>
                </div>
              );
            })}
          </div>
        </div>
      </section>

      {/* 07. THE CLOSING: Final CTA */}
      <section className="py-24 lg:py-32 bg-parchment-100 relative overflow-hidden">
        <div className="container-lux relative z-10 text-center">
          <Reveal>
            <h2 className="heading-serif text-5xl lg:text-7xl text-ink mb-8 leading-tight">
              Ready to give a gift<br />that stays?
            </h2>
            <div className="flex flex-col sm:flex-row items-center justify-center gap-4">
              <Link to="/shop" className="btn-primary text-lg px-12 py-5">
                Shop the Collections <ArrowRight size={20} />
              </Link>
              <Link to="/custom-bouquet" className="btn-secondary text-lg px-12 py-5">
                Design a Custom Piece
              </Link>
            </div>
          </Reveal>
        </div>
      </section>
    </div>
  );
}
